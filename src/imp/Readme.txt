Téma AP: Principy OOP v komunikačním softwaru postaveném na jazyku JAVA
Cíl AP: Vysvětlete a popište principy objektově orientované architektury na 
        návrh aplikace pro internetovou komunikaci. Slabá bezpečnostní místa 
        komunikace  a možnosti ošetření chyb jak za pomocí nástrojů, 
        které nabízí samotná JAVA, tak nástrojů a principů které nabízí 
        objektově orientovaná architektura (včetně příkladů některých 
        vhodných návrhových vzorů). 

//TODO

Zpracování příchozí zprávy a její zobrazení v okně JTextArea History
    -vytvoření ActionListener který tuto operaci provede.

Zpracování napsané zprávy a její zobrazení v okně JTextArea History 
    -vytvoření ActionListener a buttonBinding pro tlačítko v GUI (potřeba vytvořit)
     a pro klávesu ENTER.
    -přebrání zprávy z GUI a její předání do ModelClient.

Vylepšení funkčnosti serveru na client-client (v současné době možná komunikace pouze server-client)
    -úprava metody createSession která bude pouze předávat data
    -Vytvoření jednoduchého GUI pro server k monitorování aktivity

Zpracování ukládání zpráv a seznamu uložených lidí
    -lokální/centrální databáze
    -serializace?
    -šifrování

SSL/TLS - když zbyde čas

